name: Apply v0.13.1 Enhanced OCR

on:
  push:
    branches: [ "main" ]
    paths:
      - "scripts/upgrade_v0131_ocr.py"
      - ".github/workflows/apply-v012-assistant.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Apply enhanced OCR upgrade
        run: python3 scripts/upgrade_v0131_ocr.py

      - name: Commit upgraded sources
        run: |
          if git diff --quiet; then
            echo "No source changes required."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/src/main/java/com/aracdefteri/app/ReceiptOcr.java \
                  app/src/main/java/com/aracdefteri/app/SmartDocumentAnalyzer.java \
                  app/build.gradle
          git commit -m "Improve receipt OCR and fuel field extraction"
          git push
